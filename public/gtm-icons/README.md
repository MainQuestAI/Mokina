# 数字操作图标

24枚SVG复制自用户指定的美的数字设计基线 `10-资产 Agent/design-tokens/icons/actions/`，来源属性为 proposed，并非官方产品/功能图标。

`base/core.js` 的 ACTION_ICON_MAP 显式将已有动作名称映射到该资源；未覆盖的业务图标沿用原型自有路径，统一24 viewBox与1.5笔画。设置齿轮为本次数字操作补充，不冒充官方品牌资源。图标通过currentColor继承控件状态。
